table.w-full.border-collapse
    tbody
        each row in costSavingsHeaders
            th.text-gray-400.p-2.text-left.
                #{row}
        each row in costSavingsData
            tr
                each column in row
                    td.p-2.text-left.
                        #{column}